sass = "sass"

if [ -d $sass ]; then
  mkdir sass/css > /dev/null
fi
cd ../.. > /dev/null

echo "npm install start"
npm install
echo ""

echo "bundle install start"
bundle install
echo ""

echo "gulp start"
gulp
