echo "public/cssの中身を初期化します"
rm -rf public/css/*
echo ""

mkdir sass > /dev/null
cd .. > /dev/null

echo "npm install start"
npm install
echo ""

echo "bundle install start"
bundle install
echo ""

echo "gulp start"
gulp
